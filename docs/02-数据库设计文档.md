# 海洋生物多样性信息管理系统 — 数据库设计文档

**版本：** 1.0  
**日期：** 2026-04-21  
**数据库：** MySQL 8.0+  
**字符集：** utf8mb4

---

## 1. 实体关系图（ER 逻辑关系）

```
┌─────────────┐       ┌─────────────────┐       ┌─────────────┐
│    user     │       │     species     │       │   species   │
│  (用户表)    │       │   (物种信息表)   │       │   _image    │
└─────────────┘       └─────────────────┘       │ (物种图片表) │
                                              └─────────────┘
                                                      │
                         ┌──────────────────────────┘
                         │ 1:N
┌─────────────┐       ┌─────────────────┐       ┌─────────────────────┐
│  ecosystem  │       │   observation   │◄─────│  observation_species │
│ (生态系统表) │       │   (观测记录表)   │  1:N  │   (观测-物种关联表)   │
└─────────────┘       └─────────────────┘       └─────────────────────┘
        ▲                       ▲                          │
        │                       │                          │ N:1
        └───────────────────────┘                          ▼
                    N:1                              ┌─────────────────┐
                                                     │     species     │
                                                     └─────────────────┘
```

**关系说明：**
- `user` 与 `observation`：一个用户可创建多条观测记录（1:N）。
- `ecosystem` 与 `observation`：一个生态系统下可有多条观测记录（1:N）。
- `species` 与 `species_image`：一个物种可有多张图片（1:N）。
- `observation` 与 `species`：通过 `observation_species` 关联表实现多对多关系，同时记录关联时的扩展字段（数量、行为、备注）。

---

## 2. 数据表清单

| 序号 | 表名 | 中文名 | 说明 |
|------|------|--------|------|
| 1 | `user` | 用户表 | 存储所有注册用户及管理员信息 |
| 2 | `species` | 物种信息表 | 核心数据表，存储物种基础与分类信息 |
| 3 | `species_image` | 物种图片表 | 存储物种关联的图片URL |
| 4 | `ecosystem` | 生态系统表 | 存储海洋生态系统基础数据 |
| 5 | `observation` | 观测记录表 | 存储每次野外/实验室观测事件 |
| 6 | `observation_species` | 观测-物种关联表 | 多对多关联，含观测时的物种数量与行为 |
| 7 | `operation_log` | 操作日志表 | 记录关键用户操作，用于审计 |

---

## 3. 表结构详细定义

### 3.1 user（用户表）

| 字段名 | 类型 | 长度/精度 | 是否可空 | 默认值 | 说明 |
|--------|------|-----------|---------|--------|------|
| id | BIGINT | - | NOT NULL | AUTO_INCREMENT | 主键，用户唯一标识 |
| username | VARCHAR | 64 | NOT NULL | - | 登录用户名，唯一 |
| password | VARCHAR | 128 | NOT NULL | - | 加密后的密码（BCrypt） |
| real_name | VARCHAR | 64 | NULL | - | 真实姓名 |
| email | VARCHAR | 128 | NULL | - | 邮箱 |
| avatar | VARCHAR | 255 | NULL | - | 头像图片URL |
| role | TINYINT | - | NOT NULL | 3 | 角色：0-ADMIN, 1-RESEARCHER, 2-STUDENT, 3-PUBLIC |
| status | TINYINT | - | NOT NULL | 0 | 状态：0-待审核, 1-正常, 2-禁用 |
| create_time | DATETIME | - | NOT NULL | CURRENT_TIMESTAMP | 注册时间 |
| update_time | DATETIME | - | NOT NULL | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- UNIQUE KEY `uk_username` (`username`)
- INDEX `idx_role` (`role`)
- INDEX `idx_status` (`status`)

---

### 3.2 species（物种信息表）

| 字段名 | 类型 | 长度/精度 | 是否可空 | 默认值 | 说明 |
|--------|------|-----------|---------|--------|------|
| id | BIGINT | - | NOT NULL | AUTO_INCREMENT | 主键 |
| chinese_name | VARCHAR | 128 | NOT NULL | - | 中文名 |
| scientific_name | VARCHAR | 256 | NOT NULL | - | 学名（拉丁名），建议唯一 |
| phylum | VARCHAR | 64 | NULL | - | 门 |
| class_name | VARCHAR | 64 | NULL | - | 纲 |
| order_name | VARCHAR | 64 | NULL | - | 目 |
| family | VARCHAR | 64 | NULL | - | 科 |
| genus | VARCHAR | 64 | NULL | - | 属 |
| species | VARCHAR | 64 | NULL | - | 种 |
| morphological_features | TEXT | - | NULL | - | 形态特征 |
| living_habits | TEXT | - | NULL | - | 生活习性 |
| distribution | TEXT | - | NULL | - | 分布区域描述 |
| distribution_lat | DECIMAL | (10,8) | NULL | - | 分布中心纬度 |
| distribution_lng | DECIMAL | (11,8) | NULL | - | 分布中心经度 |
| protection_level | VARCHAR | 32 | NULL | - | 保护等级（如国家一级、二级） |
| iucn_status | VARCHAR | 32 | NULL | - | IUCN濒危状态（如CR, EN, VU, NT, LC） |
| video_url | VARCHAR | 512 | NULL | - | 视频链接 |
| references | TEXT | - | NULL | - | 参考文献 |
| is_public | TINYINT | - | NOT NULL | 1 | 是否对公众公开：0-否, 1-是 |
| create_by | BIGINT | - | NOT NULL | - | 创建人（user.id） |
| create_time | DATETIME | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | NOT NULL | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- UNIQUE KEY `uk_scientific_name` (`scientific_name`)
- INDEX `idx_chinese_name` (`chinese_name`)
- INDEX `idx_phylum` (`phylum`)
- INDEX `idx_class` (`class_name`)
- INDEX `idx_order` (`order_name`)
- INDEX `idx_family` (`family`)
- INDEX `idx_protection_level` (`protection_level`)
- INDEX `idx_iucn_status` (`iucn_status`)
- INDEX `idx_is_public` (`is_public`)
- INDEX `idx_create_by` (`create_by`)

---

### 3.3 species_image（物种图片表）

| 字段名 | 类型 | 长度/精度 | 是否可空 | 默认值 | 说明 |
|--------|------|-----------|---------|--------|------|
| id | BIGINT | - | NOT NULL | AUTO_INCREMENT | 主键 |
| species_id | BIGINT | - | NOT NULL | - | 关联物种ID |
| image_url | VARCHAR | 512 | NOT NULL | - | 图片存储URL |
| sort_order | INT | - | NOT NULL | 0 | 排序序号，越小越靠前 |
| create_time | DATETIME | - | NOT NULL | CURRENT_TIMESTAMP | 上传时间 |

**索引：**
- PRIMARY KEY (`id`)
- INDEX `idx_species_id` (`species_id`)

**外键：**
- FOREIGN KEY (`species_id`) REFERENCES `species`(`id`) ON DELETE CASCADE

---

### 3.4 ecosystem（生态系统表）

| 字段名 | 类型 | 长度/精度 | 是否可空 | 默认值 | 说明 |
|--------|------|-----------|---------|--------|------|
| id | BIGINT | - | NOT NULL | AUTO_INCREMENT | 主键 |
| name | VARCHAR | 128 | NOT NULL | - | 生态系统名称（如珊瑚礁、红树林） |
| type | VARCHAR | 64 | NULL | - | 类型标识/编码 |
| description | TEXT | - | NULL | - | 类型描述 |
| geo_range | TEXT | - | NULL | - | 地理范围描述 |
| environment_features | TEXT | - | NULL | - | 环境特征 |
| create_time | DATETIME | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | NOT NULL | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- UNIQUE KEY `uk_name` (`name`)

---

### 3.5 observation（观测记录表）

| 字段名 | 类型 | 长度/精度 | 是否可空 | 默认值 | 说明 |
|--------|------|-----------|---------|--------|------|
| id | BIGINT | - | NOT NULL | AUTO_INCREMENT | 主键 |
| observation_time | DATETIME | - | NOT NULL | - | 观测时间 |
| location_name | VARCHAR | 256 | NULL | - | 地点名称描述 |
| latitude | DECIMAL | (10,8) | NOT NULL | - | 纬度 |
| longitude | DECIMAL | (11,8) | NOT NULL | - | 经度 |
| ecosystem_id | BIGINT | - | NULL | - | 关联生态系统ID |
| observer | VARCHAR | 128 | NULL | - | 观测人员姓名（可记录多人，逗号分隔或后续扩展） |
| observer_user_id | BIGINT | - | NULL | - | 创建该记录的系统中用户ID |
| water_temperature | DECIMAL | (5,2) | NULL | - | 水温（℃） |
| salinity | DECIMAL | (5,2) | NULL | - | 盐度（‰或psu） |
| ph_value | DECIMAL | (4,2) | NULL | - | pH值 |
| depth | DECIMAL | (8,2) | NULL | - | 深度（米） |
| remarks | TEXT | - | NULL | - | 备注 |
| create_by | BIGINT | - | NOT NULL | - | 创建人（user.id） |
| create_time | DATETIME | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | NOT NULL | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- INDEX `idx_ecosystem_id` (`ecosystem_id`)
- INDEX `idx_observation_time` (`observation_time`)
- INDEX `idx_create_by` (`create_by`)
- INDEX `idx_lat_lng` (`latitude`, `longitude`)

**外键：**
- FOREIGN KEY (`ecosystem_id`) REFERENCES `ecosystem`(`id`) ON DELETE SET NULL

---

### 3.6 observation_species（观测-物种关联表）

| 字段名 | 类型 | 长度/精度 | 是否可空 | 默认值 | 说明 |
|--------|------|-----------|---------|--------|------|
| id | BIGINT | - | NOT NULL | AUTO_INCREMENT | 主键 |
| observation_id | BIGINT | - | NOT NULL | - | 观测记录ID |
| species_id | BIGINT | - | NOT NULL | - | 物种ID |
| estimated_quantity | INT | - | NULL | - | 估算数量 |
| behavior | VARCHAR | 256 | NULL | - | 行为描述 |
| remarks | VARCHAR | 512 | NULL | - | 备注 |
| create_time | DATETIME | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |

**索引：**
- PRIMARY KEY (`id`)
- UNIQUE KEY `uk_observation_species` (`observation_id`, `species_id`)
- INDEX `idx_species_id` (`species_id`)

**外键：**
- FOREIGN KEY (`observation_id`) REFERENCES `observation`(`id`) ON DELETE CASCADE
- FOREIGN KEY (`species_id`) REFERENCES `species`(`id`) ON DELETE CASCADE

---

### 3.7 operation_log（操作日志表）

| 字段名 | 类型 | 长度/精度 | 是否可空 | 默认值 | 说明 |
|--------|------|-----------|---------|--------|------|
| id | BIGINT | - | NOT NULL | AUTO_INCREMENT | 主键 |
| user_id | BIGINT | - | NULL | - | 操作用户ID |
| username | VARCHAR | 64 | NULL | - | 操作用户名（冗余，方便查询） |
| operation | VARCHAR | 128 | NOT NULL | - | 操作描述（如"删除物种"） |
| method | VARCHAR | 10 | NULL | - | 请求方法（GET/POST/PUT/DELETE） |
| request_url | VARCHAR | 256 | NULL | - | 请求URL |
| request_params | TEXT | - | NULL | - | 请求参数JSON |
| ip_address | VARCHAR | 64 | NULL | - | 操作者IP |
| status | TINYINT | - | NOT NULL | 1 | 操作状态：0-失败, 1-成功 |
| error_msg | TEXT | - | NULL | - | 错误信息 |
| create_time | DATETIME | - | NOT NULL | CURRENT_TIMESTAMP | 操作时间 |

**索引：**
- PRIMARY KEY (`id`)
- INDEX `idx_user_id` (`user_id`)
- INDEX `idx_create_time` (`create_time`)

---

## 4. 建表 SQL（MySQL）

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS marine_biodiversity
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;

USE marine_biodiversity;

-- 1. 用户表
CREATE TABLE `user` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `username` VARCHAR(64) NOT NULL COMMENT '登录用户名',
  `password` VARCHAR(128) NOT NULL COMMENT '加密密码',
  `real_name` VARCHAR(64) DEFAULT NULL COMMENT '真实姓名',
  `email` VARCHAR(128) DEFAULT NULL COMMENT '邮箱',
  `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
  `role` TINYINT NOT NULL DEFAULT 3 COMMENT '角色：0-ADMIN,1-RESEARCHER,2-STUDENT,3-PUBLIC',
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '状态：0-待审核,1-正常,2-禁用',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '注册时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  KEY `idx_role` (`role`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 2. 物种信息表
CREATE TABLE `species` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `chinese_name` VARCHAR(128) NOT NULL COMMENT '中文名',
  `scientific_name` VARCHAR(256) NOT NULL COMMENT '学名',
  `phylum` VARCHAR(64) DEFAULT NULL COMMENT '门',
  `class_name` VARCHAR(64) DEFAULT NULL COMMENT '纲',
  `order_name` VARCHAR(64) DEFAULT NULL COMMENT '目',
  `family` VARCHAR(64) DEFAULT NULL COMMENT '科',
  `genus` VARCHAR(64) DEFAULT NULL COMMENT '属',
  `species` VARCHAR(64) DEFAULT NULL COMMENT '种',
  `morphological_features` TEXT COMMENT '形态特征',
  `living_habits` TEXT COMMENT '生活习性',
  `distribution` TEXT COMMENT '分布区域描述',
  `distribution_lat` DECIMAL(10,8) DEFAULT NULL COMMENT '分布中心纬度',
  `distribution_lng` DECIMAL(11,8) DEFAULT NULL COMMENT '分布中心经度',
  `protection_level` VARCHAR(32) DEFAULT NULL COMMENT '保护等级',
  `iucn_status` VARCHAR(32) DEFAULT NULL COMMENT 'IUCN濒危状态',
  `video_url` VARCHAR(512) DEFAULT NULL COMMENT '视频链接',
  `references` TEXT COMMENT '参考文献',
  `is_public` TINYINT NOT NULL DEFAULT 1 COMMENT '是否公开：0-否,1-是',
  `create_by` BIGINT NOT NULL COMMENT '创建人',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_scientific_name` (`scientific_name`),
  KEY `idx_chinese_name` (`chinese_name`),
  KEY `idx_phylum` (`phylum`),
  KEY `idx_class` (`class_name`),
  KEY `idx_order` (`order_name`),
  KEY `idx_family` (`family`),
  KEY `idx_protection_level` (`protection_level`),
  KEY `idx_iucn_status` (`iucn_status`),
  KEY `idx_is_public` (`is_public`),
  KEY `idx_create_by` (`create_by`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='物种信息表';

-- 3. 物种图片表
CREATE TABLE `species_image` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `species_id` BIGINT NOT NULL COMMENT '关联物种ID',
  `image_url` VARCHAR(512) NOT NULL COMMENT '图片URL',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_species_id` (`species_id`),
  CONSTRAINT `fk_image_species` FOREIGN KEY (`species_id`) REFERENCES `species` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='物种图片表';

-- 4. 生态系统表
CREATE TABLE `ecosystem` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(128) NOT NULL COMMENT '名称',
  `type` VARCHAR(64) DEFAULT NULL COMMENT '类型编码',
  `description` TEXT COMMENT '描述',
  `geo_range` TEXT COMMENT '地理范围',
  `environment_features` TEXT COMMENT '环境特征',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_name` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='生态系统表';

-- 5. 观测记录表
CREATE TABLE `observation` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `observation_time` DATETIME NOT NULL COMMENT '观测时间',
  `location_name` VARCHAR(256) DEFAULT NULL COMMENT '地点名称',
  `latitude` DECIMAL(10,8) NOT NULL COMMENT '纬度',
  `longitude` DECIMAL(11,8) NOT NULL COMMENT '经度',
  `ecosystem_id` BIGINT DEFAULT NULL COMMENT '关联生态系统',
  `observer` VARCHAR(128) DEFAULT NULL COMMENT '观测人员',
  `observer_user_id` BIGINT DEFAULT NULL COMMENT '观测用户ID',
  `water_temperature` DECIMAL(5,2) DEFAULT NULL COMMENT '水温',
  `salinity` DECIMAL(5,2) DEFAULT NULL COMMENT '盐度',
  `ph_value` DECIMAL(4,2) DEFAULT NULL COMMENT 'pH值',
  `depth` DECIMAL(8,2) DEFAULT NULL COMMENT '深度(m)',
  `remarks` TEXT COMMENT '备注',
  `create_by` BIGINT NOT NULL COMMENT '创建人',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_ecosystem_id` (`ecosystem_id`),
  KEY `idx_observation_time` (`observation_time`),
  KEY `idx_create_by` (`create_by`),
  KEY `idx_lat_lng` (`latitude`,`longitude`),
  CONSTRAINT `fk_obs_ecosystem` FOREIGN KEY (`ecosystem_id`) REFERENCES `ecosystem` (`id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='观测记录表';

-- 6. 观测-物种关联表
CREATE TABLE `observation_species` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `observation_id` BIGINT NOT NULL COMMENT '观测记录ID',
  `species_id` BIGINT NOT NULL COMMENT '物种ID',
  `estimated_quantity` INT DEFAULT NULL COMMENT '估算数量',
  `behavior` VARCHAR(256) DEFAULT NULL COMMENT '行为描述',
  `remarks` VARCHAR(512) DEFAULT NULL COMMENT '备注',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_observation_species` (`observation_id`,`species_id`),
  KEY `idx_species_id` (`species_id`),
  CONSTRAINT `fk_os_observation` FOREIGN KEY (`observation_id`) REFERENCES `observation` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_os_species` FOREIGN KEY (`species_id`) REFERENCES `species` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='观测-物种关联表';

-- 7. 操作日志表
CREATE TABLE `operation_log` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT DEFAULT NULL COMMENT '用户ID',
  `username` VARCHAR(64) DEFAULT NULL COMMENT '用户名',
  `operation` VARCHAR(128) NOT NULL COMMENT '操作描述',
  `method` VARCHAR(10) DEFAULT NULL COMMENT '请求方法',
  `request_url` VARCHAR(256) DEFAULT NULL COMMENT '请求URL',
  `request_params` TEXT COMMENT '请求参数',
  `ip_address` VARCHAR(64) DEFAULT NULL COMMENT 'IP地址',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-失败,1-成功',
  `error_msg` TEXT COMMENT '错误信息',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';
```

---

## 5. 数据库设计约定

1. **主键策略**：所有表使用 `BIGINT` 自增主键，名为 `id`。
2. **时间字段**：所有业务表包含 `create_time` 和 `update_time`，由数据库自动维护。
3. **逻辑删除**：本系统采用物理删除（需求要求记录操作日志），删除前通过应用层校验关联关系。
4. **外键约束**：数据库层面建立外键，保证引用完整性；`ON DELETE` 策略根据业务场景选择 `CASCADE` 或 `SET NULL`。
5. **命名规范**：表名、字段名使用小写+下划线；实体类中使用驼峰命名，通过 MyBatis 自动映射。
6. **角色/状态枚举**：后端使用 Java Enum 定义，数据库中以 `TINYINT` 存储。
